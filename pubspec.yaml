name: dashed_divider
description: "Flutter dashed divider widget with horizontal/vertical modes, configurable dash length, gap, thickness, margin, rounded caps, and optional fixed length."
version: 1.0.0
homepage: https://jsontodart.cn/
repository: https://github.com/Matkurban/dashed_divider
topics: [widgets]

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
